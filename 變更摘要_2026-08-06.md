# 變更摘要:2026-08-06

> 範圍:2026-08-06 全天(今日 08-07 尚無新 commit)。
> 規模:6 個 commit、33 個檔案、+1495 / −38。
> 主題:把「能跑的系統」變成「開機就能用、放著不管也不會壞、換機器也不會壞」。

---

## 一、開機即用、放著不管也不壞的維運
`36cde2f` `3322fe6` `8a0c3c0`

- **開機(登入)自動上線**:註冊兩個 Windows 工作排程啟動 web 與 worker,
  資料庫交給 Docker 的 `restart: unless-stopped`;worker 啟動時自己等資料庫
  (最多兩分鐘),所以啟動順序不用人管。**不必再每次手動開兩個終端機。**
- **worker 救回孤兒單據**:pg-boss 的工作預設只保留 14 天。若關機一個月再開,
  正在「辨識中」的單據會永遠卡住。現在 worker 啟動時會掃出這些孤兒單據重新派工。
  邏輯在 core(`selectLostRecognitions`)並附測試。
- **維運手冊** `docs/operations.md`:怎麼開機、怎麼看狀態、出問題怎麼救。
- 使用模式定調為 **「要用才開電腦」**,不是 24 小時服務 —— 報帳本來就是
  回辦公室才做的事(出差先拍在相簿,回來批次上傳)。

## 二、檔案位置可搬家(ADR-0007)
`42fd827` — 這批最大的一筆,19 檔

- 憑證影像與匯出檔改成**存相對鍵值**(相對於 `UPLOAD_DIR` / `EXPORT_DIR`),
  讀取端把早期存的絕對路徑正規化;兩種格式永久並存。
- 效果:**換機器、換 Windows 使用者名稱、搬進容器、搬上 VPS,都不會讓憑證
  影像失聯,而且一列資料庫都不用改。**
- 順帶做了路徑逃逸防護(檔案必須落在指定目錄內)。
- 新增 `packages/core/src/storage-path.ts` 與 `packages/config/src/paths.ts`,
  共約 100 則測試;實測把目錄指到別處、資料庫不動,6 張影像照樣讀得到,
  刻意少放一張則正確回 404。

## 三、版控與畫面修正
`68576e8` `cb0712c`

- ⚠️ **月結匯出頁的原始碼其實一直沒進 git**:`.gitignore` 有條規則沒有錨定
  到根目錄,把匯出頁的檔案誤判忽略掉了。這批把規則修正、原始碼補進版控 ——
  等於 Phase 3 的匯出 UI 到昨天才真正進到 repo,之前只存在本機。
- 日期欄位不再溢出版面,與「單據類型」下拉選單同尺寸(手機上的小修)。
- 移除驗證過程產生的 tsconfig 雜訊。

---

## 一句話總結

昨天沒有加新功能,而是把 Phase 1–3 已完成的系統**收尾成日常可用**:
一開機就自己上線、長期關機再開也能自我修復、換任何機器都不會弄丟憑證,
並補回一塊本來漏掉版控的程式碼。
